{
  "name": "web-schema-and-data-visualiser",
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "node": "16.15.0",
    "yarn": "1.22.19"
  },
  "cacheDirectories": [
    "frontend/node_modules"
  ],
  "scripts": {
    "heroku-postbuild": "cd frontend && yarn install && yarn run build --verbose"
  }
}
