{
  "name": "web-schema-and-data-visualiser",
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "node": "16.15.x",
    "yarn": "1.22.x"
  },
  "cacheDirectories": [
    "client/node_modules"
  ],
  "scripts": {
    "heroku-postbuild": "cd frontend && yarn install && yarn run build"
  }
}
